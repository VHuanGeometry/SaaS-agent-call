<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.2.5</version>
        <relativePath/>
    </parent>

    <groupId>com.vhuan</groupId>
    <artifactId>vhuan</artifactId>
    <packaging>pom</packaging>
    <modules>
    </modules>
    <version>1.0.0-SNAPSHOT</version>

    <properties>
        <java.version>21</java.version>
        <maven.compiler.source>21</maven.compiler.source>
        <maven.compiler.target>21</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <nacos-client.version>2.3.2</nacos-client.version>
        <graceful-response.version>3.4.0-boot3</graceful-response.version>
        <springdoc-openapi.version>2.8.17</springdoc-openapi.version>
        <swagger-annotations.version>2.2.15</swagger-annotations.version>
        <knife4j.version>4.5.0</knife4j.version>
        <!-- MyBatis-Flex：轻量级 ORM 框架，支持 Spring Boot 3.x / Java 21 -->
        <mybatis-flex.version>1.9.5</mybatis-flex.version>
        <!-- MapStruct：编译期生成 PO/DTO/领域对象转换代码，性能优于反射框架 -->
        <mapstruct.version>1.5.5.Final</mapstruct.version>
        <lombok-mapstruct-binding.version>0.2.0</lombok-mapstruct-binding.version>
        <!-- netty-socketio：基于 Netty 的 Socket.IO 服务端实现，用于实时房间通信 -->
        <netty-socketio.version>2.0.12</netty-socketio.version>
        <!-- Redisson：为 netty-socketio 提供多实例 Redis Pub/Sub 存储适配器（RedissonStoreFactory） -->
        <redisson.version>3.27.2</redisson.version>
        <!-- jjwt：JWT Token 生成与解析（兼容 Spring Boot 3.x / Java 21） -->
        <jjwt.version>0.12.6</jjwt.version>
    </properties>

    <dependencyManagement>
        <dependencies>


            <!-- netty-socketio: Socket.IO 服务端（Redis 存储支持已内置在主 jar 中） -->
            <dependency>
                <groupId>com.corundumstudio.socketio</groupId>
                <artifactId>netty-socketio</artifactId>
                <version>${netty-socketio.version}</version>
            </dependency>

            <!-- Redisson: 为 netty-socketio 的 RedissonStoreFactory 提供多实例 Redis Pub/Sub 适配器 -->
            <dependency>
                <groupId>org.redisson</groupId>
                <artifactId>redisson</artifactId>
                <version>${redisson.version}</version>
            </dependency>


            <!-- Nacos Client -->
            <dependency>
                <groupId>com.alibaba.nacos</groupId>
                <artifactId>nacos-client</artifactId>
                <version>${nacos-client.version}</version>
            </dependency>

            <!-- Graceful Response -->
            <dependency>
                <groupId>com.feiniaojin</groupId>
                <artifactId>graceful-response</artifactId>
                <version>${graceful-response.version}</version>
            </dependency>


            <dependency>
                <groupId>com.github.xiaoymin</groupId>
                <artifactId>knife4j-openapi3-jakarta-spring-boot-starter</artifactId>
                <version>${knife4j.version}</version>
            </dependency>

            <dependency>
                <groupId>io.swagger.core.v3</groupId>
                <artifactId>swagger-annotations</artifactId>
                <version>${swagger-annotations.version}</version>
            </dependency>

            <!-- MyBatis-Flex Spring Boot Starter（统一管理版本） -->
            <dependency>
                <groupId>com.mybatis-flex</groupId>
                <artifactId>mybatis-flex-spring-boot-starter</artifactId>
                <version>${mybatis-flex.version}</version>
            </dependency>

            <!-- MyBatis-Flex APT 处理器（编译期生成 TableDef） -->
            <dependency>
                <groupId>com.mybatis-flex</groupId>
                <artifactId>mybatis-flex-processor</artifactId>
                <version>${mybatis-flex.version}</version>
            </dependency>

            <!-- MyBatis-Spring：强制锁定到 Jakarta 命名空间版 3.0.x，
                 以兼容 Spring Framework 6.1（避免旧的 mybatis-spring 2.1.2 将
                 factoryBeanObjectType 写成 String 而触发 IllegalArgumentException） -->
            <dependency>
                <groupId>org.mybatis</groupId>
                <artifactId>mybatis-spring</artifactId>
                <version>3.0.3</version>
            </dependency>

            <!-- MyBatis Core：与 mybatis-spring 3.0.3 配套使用 -->
            <dependency>
                <groupId>org.mybatis</groupId>
                <artifactId>mybatis</artifactId>
                <version>3.5.16</version>
            </dependency>

            <!-- PostgreSQL JDBC 驱动版本已由 spring-boot-starter-parent 统一管理 -->

            <!-- MapStruct：编译期生成对象转换代码 -->
            <dependency>
                <groupId>org.mapstruct</groupId>
                <artifactId>mapstruct</artifactId>
                <version>${mapstruct.version}</version>
            </dependency>

            <!-- jjwt：JWT 生成与解析（api 编译期依赖，impl/jackson 运行时依赖） -->
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-api</artifactId>
                <version>${jjwt.version}</version>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-impl</artifactId>
                <version>${jjwt.version}</version>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-jackson</artifactId>
                <version>${jjwt.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>
</project>
